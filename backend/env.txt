# ===========================================
# COFFEE NI JEEM - RENDER DEPLOYMENT CONFIG
# ===========================================
# This is a template for production deployment on Render
# Copy these to your Render Environment Variables

# App Configuration
APP_NAME=Coffee Ni Jeem
APP_ENV=production
APP_KEY=
APP_DEBUG=false
APP_URL=https://your-app-name.onrender.com
LOG_CHANNEL=stderr
LOG_LEVEL=error

# Database Configuration (PostgreSQL from Render)
# Get these from your Render PostgreSQL database
DB_CONNECTION=pgsql
DB_HOST=
DB_PORT=5432
DB_DATABASE=
DB_USERNAME=
DB_PASSWORD=

# Session & Cache
SESSION_DRIVER=database
SESSION_LIFETIME=120
SESSION_ENCRYPT=false
SESSION_DOMAIN=
SESSION_SECURE_COOKIE=true
SESSION_SAME_SITE=none
CACHE_STORE=database
QUEUE_CONNECTION=database

# Frontend Configuration
FRONTEND_URL=https://brew-with-jeem.netlify.app

# Sanctum Configuration
# IMPORTANT: Replace with your actual Render URL and Netlify domain
SANCTUM_STATEFUL_DOMAINS=brew-with-jeem.netlify.app

# Mail Configuration (Optional - configure if needed)
MAIL_MAILER=log
MAIL_HOST=
MAIL_PORT=587
MAIL_USERNAME=
MAIL_PASSWORD=
MAIL_ENCRYPTION=tls
MAIL_FROM_ADDRESS=noreply@example.com
MAIL_FROM_NAME="${APP_NAME}"

# ===========================================
# INSTRUCTIONS FOR RENDER DEPLOYMENT:
# ===========================================
# 1. Create PostgreSQL database on Render
# 2. In your web service, go to Environment tab
# 3. Click "Add from Database" and select your PostgreSQL
#    This will auto-populate: DB_HOST, DB_PORT, DB_DATABASE, DB_USERNAME, DB_PASSWORD
# 4. Manually add these CRITICAL variables:
#    - APP_KEY=(generate with: php artisan key:generate --show)
#    - APP_NAME=Coffee Ni Jeem
#    - APP_ENV=production
#    - APP_DEBUG=false
#    - LOG_CHANNEL=stderr
#    - LOG_LEVEL=error
#    - SESSION_DRIVER=database
#    - SESSION_ENCRYPT=false
#    - SESSION_SECURE_COOKIE=true
#    - SESSION_SAME_SITE=none
#    - CACHE_STORE=database
#    - FRONTEND_URL=https://brew-with-jeem.netlify.app
#    - SANCTUM_STATEFUL_DOMAINS=brew-with-jeem.netlify.app
# 5. Clear cache after adding variables (run Manual Deploy or redeploy)
# 6. Check logs if 500 error or 419 CSRF error occurs